<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.travelhelper">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TravelHelper"
        android:usesCleartextTraffic="true">
        <activity android:name=".mvp.view.CreateRoomActivity"></activity>
        <activity android:name=".mvp.view.RoomEditActivity" />
        <activity android:name=".mvp.view.RoomOverviewActivity" />
        <activity android:name=".mvp.view.HotelEditActivity" />
        <activity android:name=".mvp.view.HotelDetailsActivity" />
        <activity android:name=".mvp.view.CreateHotelActivity" />
        <activity android:name=".mvp.view.RegistrationActivity" />
        <activity android:name=".mvp.view.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvp.view.HomeActivity" />
        <activity android:name=".mvp.view.AdminActivity" />
    </application>

</manifest>